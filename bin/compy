#!/usr/bin/env node

var path = require('path');
var fs   = require('fs');
var root  = path.join(path.dirname(fs.realpathSync(__filename)), '..');

require('grunt').cli({gruntfile: root + "/Gruntfile.js", targetBase: process.cwd(), base: process.cwd(), verbose: false});
